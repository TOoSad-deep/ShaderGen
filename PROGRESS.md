# 进度

最后更新：2026-07-15

## 当前状态

Learn Harness Engineering 方法的阶段 1 已完成：最小仓库事实来源已经建立，并已通过验证。最小在线 Review 节点已完成：Agent 内部通过 `shader_generation_graph` 串联 `generate_glsl` 和 `review_render` 两个节点；前端在 WebGL 预览第一帧后提交原图、当前渲染图和 GLSL，后端调用 Agent 评审并返回渲染评估与代码修改建议。

F09 已完成 M0–M5 工程实现、M6.0 可靠性修复和一轮 M6.1 离线质量改进。生产候选池现在会在首个成功 model best 后加入一次通用 `measurement_affine_seed_v1` 独立根候选；它不读取 benchmark case/manifest/gate，不消耗模型或视觉迭代预算，仍由 Validator、真实 WebGL1、Oracle 和 Selector 裁决。生产评分会追加 Analyst 的语义 ROI，Critic Review 按轮次留档；Candidate provenance 和 M5 runner 均严格区分 model/deterministic 来源。页面仍默认 `legacy`，`procedural_v1` 只作为实验模式显式选择。新正式 run `m5-20260715T023445Z` 的 12 项自动质量检查全部通过，但独立人工 final 偏好率只有 `3/10=30%`，低于冻结的 50% 门槛，最终 gate 为 `failed`；F09 继续 no-go，不得标记 `passing`。

`PNG-to-Shader V1 Node Lab API` 的阶段 A–D 已完成，并已收敛为“Node 是唯一节点语义实现 + Harness 只依赖通用 Provider”。`agent.app.lab` 现在不导入任何具体 Node/Graph，也不维护 PNG-to-Shader 节点 ID、descriptor 或 dispatch；生产侧 `agent.app.nodes.integrations.node_lab` 通过 `NodeProvider` 暴露 pipeline id、20/20 descriptor、execution-mode binding、routing capability 和 benchmark 源文件。15 个非模型节点仍通过不透明 Artifact facade 直接调用生产 Node factory/routing，五个模型节点调用生产角色 Node factory 与 bounded wrapper。新 JSON-safe Node 可用 `DirectNodeExecutor` 零适配接入；复杂 Node 只在生产 Provider 内添加 Adapter，不修改 Lab 内核或 Service。LabRun 与 Provider pipeline 强绑定，跨 pipeline 读取 fail closed。图片、完整 GLSL、ContextPack、mock 原文和诊断仍使用不透明 Artifact，`project_commit` 在副作用前拒绝。人工可使用 `/lab` 工作台、Swagger 或逐节点 CLI；HTTP/Swagger 默认关闭，batch 只接受三个仓库内 AI-off suite。

模块 benchmark 已覆盖 capability、真实 node target、scenario/pipeline、Renderer cold/warm、HTTP transport 与五个模型角色。模型 runner 冻结 requested model、价格版本和 provider 输出 token cap，恢复时从持久证据累计 semantic/repair/token/wall/cost，用独立 interruption 保留事故分母；报告按角色聚合 Parser/Schema/binding/timeout、latency、token、费用与 requested/actual model，样本少于 20 时 p95 为 `null`。所有 benchmark CLI 只输出稳定单行 suite/status/report path，case 失败直接返回非零。M5 发布 benchmark、gate、盲评和 output root 继续与 Node Lab 独立；Node Lab 不写产品 run 或真实 Memory，不改变 F09 active 状态和 M5 no-go 结论。

权威架构输入：

- `human_doc/shaderforge-technical-architecture-aligned(1).svg`

仓库事实来源文件：

- `README.md`
- `AGENTS.md`
- `Makefile`
- `docs/ARCHITECTURE.md`
- `docs/DECISIONS.md`
- `docs/FEATURES.md`
- `PROGRESS.md`
- `src/agent/ARCHITECTURE.md`
- `src/agent/app/*/ARCHITECTURE.md`
- `frontend/README.md`
- `backend/README.md`

## 当前 active 功能

- `F09`：PNG 转无贴图 Shader Agent V1。M0–M5、M6.0、M6.1 离线质量改进和 Node Lab API v1 阶段 A–D 已完成；新正式 run 的自动质量门禁通过、独立人工偏好门禁失败，因此继续保持 active，禁止灰度。

## 下一个产品功能

- `F09 M6.2 人类偏好对齐`：保留 `m5-20260715T023445Z` 的完整失败证据，在 Node Lab 中优先定位 topology、实例数量、高光/阴影语义层保留和 Selector 结构感知不足；重点解释 deterministic affine seed 为何在自动 objective 上晋升、但在 `rimmed_disk`、`arc_highlight_orb`、`dual_disks`、`pink_gel` 上被人工选择 initial。不得移动冻结阈值，也不得复用当前人工结果宣称修复通过；完成离线回归后必须用新 suite-run-id 重跑真实模型和独立盲评。服务端任务化/取消等可靠性缺口另立后续设计，不与本次质量改进混写。

## 验证

- 2026-07-15：完成 `codex/architecture-small-risks` 未提交改动的 review 修复。`PngToShaderV1Service.invoke()` 现在进入 Graph 前强制校验参数与 State 的 `project_id` 一致，并使用 State 绑定的 project/run key 执行 Renderer 兜底关闭，避免错误 key 造成资源泄漏；对应回归证明不一致输入不会启动 Graph。Renderer 生命周期注释已从 legacy `shader_generation_graph` Mermaid 移到正确的 `png_to_shader_v1_graph` 区块；H01 测试删除与动态 Graph 数量冲突的固定“1 个 graph”断言。验证：相关 Agent Service、Harness、Backend 和跨层 API 共 `43 passed`；Ruff、docs-check、LangGraph validate（3 个 Graph）和 `git diff --check` 通过。未改变 Graph Node、边、路由、循环或终止语义，未调用真实模型。

- 2026-07-15：在分支 `codex/architecture-small-risks` 完成第二优先级架构小隐患优化。`POST /api/shader/generate` 的项目锁、模式/模型选择、Legacy timeout、Agent 调用、生成总账、失败分类和 `ShaderResponse` 契约已从 926 行 Route 下沉到独立 Backend 用例服务 `app/services/shader_generation.py`，Route 收缩为 315 行并只保留 HTTP 校验、依赖装配和 error envelope 映射；新增 AST 边界测试防止生成编排回流 Route。V1 Graph Builder 与 Agent Service 现在共享 run 级 Renderer registry：正常路径继续由 `finalize` 关闭，Graph 外异常由 Service `finally` 限时、幂等兜底，清理失败不覆盖 Graph 结果且不记录底层原文。未新增、删除或重命名 Graph Node，未改变边、路由、循环、终止路径或 `current_best`；ASCII/Mermaid/安全说明已同步。验证：聚焦单元/跨层 API `40 passed`；`make check` 通过（364 个单元测试、docs-check、3 个 LangGraph、Frontend production build）；沙箱外关闭 LangSmith 上报后全量集成 `30 passed, 1 skipped`，真实 Chromium WebGL 用例通过；全仓 Ruff 与 `git diff --check` 通过。`make test-memory-postgres` 因当前环境未提供 `TEST_DATABASE_URL` 或可创建临时库的 `DATABASE_URL` 未执行，缺口保留；本次未调用真实模型。

- 2026-07-15：完成当前事实与默认验证语义的小范围修正。根 README 已切换到正式 run `m5-20260715T023445Z` 的真实结论：自动检查 12/12 通过、独立人工 final 偏好率 30% 未过冻结门槛、最终 gate failed/no-go；`make check` 统一称为默认主干验证并明确不包含集成、E2E、PostgreSQL 或 benchmark。H01 更新为当前 3 个 Graph，`docs_check.py` 改从 `langgraph.json` 动态读取注册数量并新增防回归测试；Node Lab 当前说明修正为 20 节点；V1 Graph 邻近 ASCII 图删除不存在的 `candidate_portfolio` 标签，未修改任何 Node、边、路由或运行逻辑。验证：聚焦 harness/Node Lab `26 passed`；`make check` 通过（361 个单元测试、docs-check、LangGraph validate 发现 3 个 Graph、Frontend production build）。

- 2026-07-15：完成 Node Lab Provider 解耦重构。将 PNG-to-Shader 的 descriptor registry、确定性/模型 Adapter 从 `agent.app.lab` / `agent.app.services` 移到生产侧 `agent.app.nodes.integrations.node_lab`；新增通用 `NodeProvider`、`NodeExecutorBinding`、`NodeExecutionHost` 和 `DirectNodeExecutor`，Service 只注入公共 Provider。Registry/pipeline 从固定 `png_to_shader_v1` 改为 Provider 驱动，LabRun 跨 pipeline 读取 fail closed；benchmark 生产源文件也由 Provider 声明。新增独立 `custom_pipeline/echo` 回归，证明无需修改 Lab 代码即可接入 JSON-safe Node；架构边界测试要求 `lab/` 对 `agent.app.nodes` / `agent.app.graphs` 零 import。验证：`make check` 通过（360 个单元测试、docs-check、3 个 LangGraph、Frontend build）；全量集成 `30 passed, 1 skipped`；全仓 Ruff、`mypy --strict src`（101 个源文件）和 `git diff --check` 通过。串行真实 Chromium benchmark：capability/node `node-lab-da0196a53ae5` 为 8/8，scenario/pipeline `node-lab-b49a12a33742` 为 2/2，warm Renderer `node-lab-c32de667b571` 为 20/20，transport `transport-ee01e2790229` 为 20/20，correctness 均为 1.0；模型 fixture `node-lab-model-290618dfb85a` 为 5/5。首轮误并发启动的三组 Chromium suite 因沙箱 Renderer 资源竞争失败，失败证据保留，随后沙箱外串行复验全部通过。重构后的真实模型五角色调用因会向外部供应商发送仓库 Prompt、参考图和测试上下文，被当前外发策略拒绝；本次没有发出调用或生成新 real run，上一次真实 run `node-lab-model-real-review-20260715-v1` 继续完整保留。未修改 Graph 节点/边、M5 gate 或 F09 active/no-go 状态。

- 2026-07-15：完成新正式 run `m5-20260715T023445Z` 的独立人工盲评和最终 gate。评审原始 JSON 以原字节归档为 `blind-review/human-review.json`，SHA-256 为 `74e02ac9e423637938b182fa3767c53c148058ec1dfcd4adf147c0e1191cc782`；schema v1、suite run、非空 reviewer、10 个唯一 case 与 `A | B | TIE` 选择均通过严格校验，config SHA-256 仍为 `5f1765218265c6b71f49c716747d31ee5288708d6c4568efd3677852dc2b7c8f`。自动检查 12/12 继续通过，人工完整度 `10/10` 通过；解码结果 final/initial/tie 为 `3/4/3`，final 偏好率 `30%` 低于冻结的 50%，唯一失败 check 为 `human_final_preference_rate`，最终 gate 明确为 `failed`。final 获选 case 为 `ellipse_gradient`、`color_lobes`、`rounded_rect_glow`；initial 获选 case 为 `rimmed_disk`、`arc_highlight_orb`、`dual_disks`、`pink_gel`；其余三项平局。`make benchmark-gate` 因 `--require-gate-passed` 按预期返回非零，报告正常生成；未调用模型、未移动阈值、未删除或覆盖失败产物，F09 保持 active/no-go。

- 2026-07-15：完成 Node Lab 语义去重后的复审与加固。修复三项问题：model Candidate 物化此前未完整交叉校验 author/mode/prompt/generator 证据；策略晋升 preview/real 未把 final 的 project/run/candidate/GLSL/render/score 重新绑定 current_best；单步 `render_and_evaluate` 沿用生产 run 级 Renderer registry，调用方不执行 finalize 时可能残留资源且 browser launch 计数不精确。现由生产 Node 统一执行前两类证据校验，Service 只为单步 Node 注入 cold Renderer 生命周期，生产 Graph 的 run 级复用不变。新增 tamper 与资源关闭回归后，定向生产 Node/Graph/Node Lab `50 passed`，Node Lab 全量 `100 passed`；五角色离线 fixture benchmark `node-lab-model-23817edaf29e` 通过。`make check` 通过（359 个单元测试、docs-check、3 个 LangGraph、Frontend production build），全量集成 `30 passed, 1 skipped`，全仓 Ruff、`mypy --strict src`（97 个源文件）和 `git diff --check` 通过。用户在数据外发风险告知后明确授权，真实模型 run `node-lab-model-real-review-20260715-v1` 使用 `dashscope:qwen3.7-plus`，五个角色 `5/5` completed/correctness passed、0 timeout、0 JSON repair；五次 semantic 调用共 17111 input、6451 output、23562 total tokens，报告上界价格估算为 `$0.36464`。四个角色 Parser 首次通过；Initial Author 因固定绑定字段返回一次 `binding_mismatch`，由既有受限本地 `shader_author_initial_fixed_bindings_v1` 修复后成功，因此总 parse pass rate 为 `0.8`，不是模型原始输出 `5/5` 合法。requested/actual model 均为 `dashscope:qwen3.7-plus`，31 个自包含 Artifact 全部通过 SHA-256 复核。未改变 Graph 节点/边、M5 gate 或 F09 active/no-go 状态。

- 2026-07-15：完成 Node Lab 节点语义去重构。删除 `lab/adapters.py` 中五个生产 Node 的平行执行路径，将 `StageCDeterministicExecutor` 改为长期职责名 `DeterministicNodeExecutor` 并扩为 15 个 deterministic descriptor 的统一生产 Node 调用器；新增 Lab ArtifactStore facade，把生产 ref 映射为不透明 id。Candidate/GLSL、确定性 seed、reference/measurements 等绑定校验移回生产 Node；selection 拒绝路径返回完整 current_best 快照；策略晋升 real/preview 共用生产计划构造。Node Lab 全量聚焦 `96 passed`，生产 Node/Graph/架构边界回归 `45 passed`；`make check` 通过（358 个单元测试、docs-check、3 个 LangGraph、Frontend production build），全量集成 `30 passed, 1 skipped`，全仓 Ruff、`mypy --strict src`（97 个源文件）和 `git diff --check` 通过。未改变 Graph 节点/边、M5 gate、真实模型开关或 F09 active/no-go 状态，未调用真实模型。

- 2026-07-15：完成 M5 人工盲评证据链加固。新 run 的公开 reviewer 目录与私有 assignment 分离，source/assets/index/template/assignment 进入 SHA-256 manifest，首次 report 锚定 manifest，evaluate 在读取人工 JSON 和覆盖报告前复验全部锚点；历史 run 不补签、不改写，继续走确定性 legacy verifier。当前 `m5-20260715T023445Z` 与旧正式 `m5-20260713-balanced-v3` 均已只读兼容复验通过。为当前 run 额外导出不参与 gate 的 reviewer-only ZIP，SHA-256 为 `1ffe6448303d19a5908f4e2250f3b5cc2aa9e3eb8ccf6e5004cc248699e6656f`；压缩包 CRC 完整且只含页面与 30 张图片。验证：聚焦 benchmark 单测 `38 passed`；`make check` 通过（357 个单元测试、docs-check、3 个 LangGraph、Frontend production build）；目标 Ruff、strict mypy 通过；用冻结 case 副本在 `/tmp` 生成新式包后，真实 Chromium 确认 0/10、下载禁用且页面和 30 张图片请求全部 HTTP 200。未调用模型、未改 gate 阈值、未修改现有 benchmark 产物。

- 2026-07-15：完成 F09 M5 新正式真实模型自动验收。固定 run `m5-20260715T023445Z` 使用审计模型 `dashscope:qwen3.7-plus`，在整套 80 次硬预算内实际计入 62 次调用；10/10 AI-off 与 AI-on final 均通过静态检查和真实 Chromium WebGL1 编译，10/10 traceability、final=current_best 和 best 单调性通过，8/10 initial-model 到 final 达到至少 0.005 的同口径改善，pink-gel bbox/global color/四个关键 ROI 全部通过冻结阈值，自动检查 12/12 通过。输入/输出 token 为 296752/77370；`solid_circle` Critic 和 `pink_gel` refine 各有一次超时，均按预算计数、保留证据且未破坏安全 finalize。盲评包已生成；只读 Chromium smoke 确认页面初始 0/10、下载禁用、10 组 30 张图片请求全部 HTTP 200、浏览器零错误。该次自动验收结束时状态严格为 `pending_human_review`，其后的独立人工结果与最终失败结论记录在上方；F09 始终保持 active/no-go。

- 2026-07-15：完成 F09 M6.1 离线质量改进。新增与 benchmark/gate 无感知的 `measurement_affine_seed_v1` 领域 API 及稳定 provenance，把它作为一次性独立根候选接入生产 Graph；补齐 model/deterministic 来源约束、失败不消耗模型 repair/stagnation、语义 ROI 追加、Review 逐轮留档和 accepted/rejected/compile-failed/refine-from-seed 分支。M5 config/report 升级 schema v3，model initial/final 使用同一 manifest objective，bbox 对 manifest expected，人工 review/assignments 与 bit-identical 证据严格校验；AI-off Markdown 不再伪报 AI-on/盲评状态。Node Lab 升级为 20/20 节点、15 个 deterministic Executor，并可单步执行/benchmark seed。验证：`make check` 通过（349 个单元测试、docs-check、3 个 LangGraph、Frontend production build）；全量集成 `30 passed, 1 skipped`；Node Lab 全量 `96 passed`；全仓 Ruff lint、`mypy --strict src`（97 个源文件）和 `git diff --check` 通过；固定 10 例真实 Chromium seed、生产 Graph 语义 ROI/pink-gel 局部门禁均通过；新 M5 AI-off suite `m5-20260715T023025Z` 为 static/compile `10/10`，且报告正确标记为 `not-evaluated`、不生成盲评包。未调用真实模型、未覆盖旧正式产物；旧正式 gate 仍为 `failed`，F09 保持 active/no-go。

- 2026-07-15：完成 M5 benchmark objective 口径修正。新 config/report schema v3 将首个成功 `origin=model` 候选固定为 initial，允许带 `generator_version` 的 deterministic 候选成为 final，并对 initial/final 冻结 PNG 使用同一套 manifest `key_rois` 重算 `manifest_key_rois_v1`；生产 Selector 内部 loss 改为独立诊断字段。bbox gate 改为直接比较 candidate 与 manifest `expected_foreground_bbox_uv`；缺少 model initial 时明确不可比较且不生成盲评包。人工 review/assignments 严格校验和 bit-identical 诊断保持不变，冻结阈值和旧正式 output 均未修改。验证：M5 benchmark 单元测试 `29 passed`；真实 Chromium benchmark/measurement-affine 集成测试 `2 passed`；目标 Ruff、docs-check 与 `git diff --check` 通过；runner 严格 mypy 在排除共享依赖已单独报告的 arg-type/unused-ignore 后通过；临时 pink-gel AI-off smoke 生成 config/report schema v3，bbox error `0.0166667`。旧正式 run 只读复算仍为 `failed`，人工 final 偏好率仍为 `0.1`。未调用真实模型，未覆盖旧 benchmark 产物；本次没有新的 AI-on/人工证据，因此 F09 继续保持 active，不能标记 passing。
- 2026-07-15：完成 `F09 Node Lab` 阶段 D。保留 M5 冻结发布证据链独立，不改其 manifest/gate/suite/盲评；Node Lab 新增 `/lab` 工作台、19 节点机器可读输入示例、五个模型 Parser 拒绝 Fixture、逐节点 Application API CLI、HTTP/OpenAPI 命名示例、步骤 DAG 摘要和 Artifact descriptor 列表。deterministic runner 新增真实 node target 与 `base_step_id` pipeline；source fingerprint 扩展到 deterministic service、production nodes、Prompt 和 Parser。模型 runner 新增 provider `max_output_tokens`、requested model/价格版本冻结、调用前 token/cost/wall 预留、中断后累计预算恢复、五角色诊断/用量/身份聚合及小样本 p95 抑制；deterministic/model/transport CLI 统一稳定单行摘要和 `0/1/2/3/130` 退出码。验收结果：Node Lab 单元/集成聚焦 `91 passed`，LLM/M5 边界回归 `55 passed`；`make check` 通过（315 个单元测试、docs-check、3 个 LangGraph、Frontend production build）；全量集成 `24 passed, 1 skipped`；`mypy --strict src/` 95 个源文件与 7 个 Stage D Backend/CLI 文件通过；全量 Ruff 和 `git diff --check` 通过。最终 AI-off capability/node `8/8`、scenario/pipeline `2/2`、Renderer warm `20/20`、transport `20/20`，correctness 均为 1.0；模型 fixture `5/5`、parse pass rate 1.0。Node Lab、procedural_v1 和 Memory 三条真实 Chromium E2E 均通过，页面恢复后可重建两步分支 DAG 与 Artifact 列表。未调用真实模型、未写产品 run/Memory；F09 与 M5 no-go 不变。
- 2026-07-15：强化 Graph 可视化的自动维护入口文档。根级 `AGENTS.md` 将节点、边、routing、循环、终止、`current_best` 和图注册变化定义为必须原子同步 ASCII/Mermaid/路由表的硬约束；`src/agent/README.md` 新增 Graph 可视化完成定义；`src/agent/app/graphs/ARCHITECTURE.md` 新增触发条件、五步维护流程及 AST 自动检查边界。实现、架构和功能状态未改变。验证：`make docs-check` 通过，harness 定向测试 11 passed，`git diff --check` 通过。
- 2026-07-14：完成 `F09 Node Lab` 阶段 C 和阶段 D 边界评估。新增 exact Executor 注入与 descriptor 前后置校验；九个 deterministic Adapter；五个模型节点的 production Prompt/Parser preview、fixture/mock/real；只读 ContextPack Artifact、Memory 晋升 preview 和 `project_commit` fail-closed；render/select production evidence binding；独立五角色模型 benchmark manifest/runner/原子 attempt/JSON+Markdown report。Backend 只通过 `agent.app.services.*` 装配默认 Gateway，HTTP real 需要服务端和请求双开关，batch 继续禁止 real。阶段 D 决定不改写 M5 冻结证据链、不创建可选前端。`make benchmark-node-lab-model` 离线 fixture 为 5/5；`make benchmark-node-lab-ai-off` 的 capability/scenario/warm/transport 分别为 8/8、1/1、20/20、20/20，correctness 均为 1.0；Node Lab 聚焦测试 34 passed，模型 Adapter/benchmark 17 passed；全量集成测试在关闭 LangSmith test tracking 并允许真实 Chromium 后 23 passed、1 skipped；`make check` 通过（296 个单元测试、docs-check、3 个 LangGraph、Frontend production build）；全仓 Ruff、严格 mypy（95 个源文件）与 `git diff --check` 通过。未调用真实模型，未写产品 run/Memory；F09 与 M5 no-go 不变。

- 2026-07-14：完成 `F09 Node Lab` 阶段 B.2，并由此收口整个阶段 B。新增五步 `normalize -> measure -> validate -> render -> evaluate` scenario 与前序响应 JSON Pointer binding；新增 `warm_per_suite` Renderer session、独立 warmup/rewarmup、20 次 measured p95；新增普通失败、取消/键盘中断现场、失败分母和不可覆盖恢复，主 runner 与 transport runner 均按相同原则保留证据；新增三个固定 AI-off suite allowlist、默认关闭 HTTP 下的 suite discovery/校验/同步 batch/report、同 id 进程内串行与配置漂移 409；新增 direct-vs-HTTP transport runner，分别记录 direct/HTTP upload、execute、download、total 和 transport overhead，规范化排除不透明 id/时间后验证领域语义与下载 hash。最终代码上的真实 Chromium `make benchmark-node-lab-ai-off` 全绿：capability cold suite `node-lab-46d353da5125` 为 `8/8`、scenario `node-lab-9c74725da04c` 为 `1/1`、warm suite `node-lab-5c84c08daebc` 为 `20/20`、transport `transport-1a2f6e5dfdb9` 为 `20/20`，四组 correctness 均为 `1.0`；warm p50/p95/max 为 `108.568354/208.472385/287.200666ms`，warmup 记录一次 browser launch，measured 均为 0；transport HTTP total p50/p95/max 为 `91.257563/114.174202/116.557ms`，overhead p50/p95/max 为 `10.025729/18.045379/21.895334ms`。`make check` 通过（259 个单元测试、docs-check、3 个 LangGraph、Frontend production build）；全量集成测试 19 passed、1 skipped；全仓 Ruff、严格 mypy 97 个源/边界文件与 `git diff --check` 通过。未调用真实模型、未写产品 run/Memory；该阶段快照当时尚未实现 C–D，现已由上方阶段 C/边界评估条目收口；F09 与 M5 no-go 不变。

- 2026-07-14：完成 `F09 Node Lab` 阶段 B.1 确定性/AI-off/HTTP 切片。新增八个复用 `shaderforge.public` 与生产纯路由函数的 capability Adapter、五个 deterministic 节点 Adapter、真实输入上限和实现状态 descriptor；原始 compiler/console 文本只写私有 diagnostics Artifact，公共响应不泄漏原文。新增固定八例 manifest、AI-off runner、精确 resume、失败分母、覆盖 Lab/生产路由/ShaderForge 契约与依赖版本的 source/environment fingerprint、JSON/Markdown report、comparison、CLI 和 Make target；每个 attempt 把输入/输出 Artifact 复制进 suite 并重新校验 SHA-256，使报告不依赖临时 Lab root。Backend 新增默认关闭的 `/api/lab/v1/*`：支持 discovery、LabRun、步骤、capability 和同 LabRun Artifact 上传/下载，默认环境 OpenAPI 中不存在；HTTP body 校验也使用不回显字段原值的稳定错误 envelope，HTTP 与 Application API 语义 parity 及跨 Backend -> Agent -> ShaderForge 集成检查通过。最终真实 Chromium suite `stage-b-evidence-20260714` 为 `8/8` attempts、correctness `1.0`，12 个自包含 Artifact 全部通过 hash 复核；profile 为 `micro/node/renderer_cold`，p50 `4.891937ms`、max `403.776833ms`，样本不足 20 次所以 p95 按契约为 `null`。`make check` 通过（255 个单元测试、docs-check、3 个 LangGraph、Frontend production build）；Node Lab 跨层集成测试 1 passed；全仓 Ruff、`mypy --strict src/`（91 个源文件）和 `git diff --check` 通过。未调用真实模型、未写产品 run/Memory。该 B.1 快照当时尚缺 scenario/transport benchmark、warm Renderer、HTTP batch、中断恢复和自动 benchmark-flow 集成测试，现已由上方 B.2 条目补齐；F09 与 M5 no-go 不变。

- 2026-07-14：完成 `F09 Node Lab` 阶段 A 共享 Harness 内核。新增 `agent.app.lab` 的严格 Schema/error、19 节点 allowlist Registry、Fixture Registry、原子 LabRun/步骤/Artifact Store 和可注入 Application API，并以 `agent.app.services.node_lab` 作为唯一公共入口；`pyproject.toml` 和 Agent/Service 架构文档已同步。最小 `decide_after_render` Fixture 带版本和 SHA-256，贯通 root/child 分支、父快照不变、重启恢复、执行 fingerprint、不透明 Artifact、跨 LabRun 拒绝、real executor fail-closed 和失败异常脱敏保存；架构测试锁定 `lab/` 不依赖 Backend、FastAPI、Graph、具体 LLM 或 Node。`make check` 通过（243 个单元测试、docs-check、3 个 LangGraph、Frontend production build）；全仓 Python Ruff、`mypy --strict src/`（88 个源文件）和 `git diff --check` 通过。未调用真实模型或 Renderer，未创建 Backend Route、CLI/benchmark、HTTP/前端，也未写产品 run 或真实项目 Memory；F09 和 M5 no-go 状态不变。

- 2026-07-14：完成 `PNG-to-Shader V1 Node Lab API` 设计并根据用户补充扩展为人工、Agent/Codex 自动化、模块化测试和 benchmark 共用 Harness，规格写入 `docs/superpowers/specs/2026-07-14-node-lab-api-v1-design.md`，并更新 D028。设计以通用 allowlist 步骤接口覆盖生产图 19/19 节点，另外拆出 normalize/measure/Validator/Renderer/Oracle/Selector/routing/context 友好接口；定义 Python Application API 真相源、HTTP/CLI 等价入口、LabRun、不可变 `base_step_id` 步骤 DAG、JSON-safe State snapshot、Artifact 引用、统一响应/error envelope、机器可读 descriptor、prompt preview、fixture/mock/real 模式和双重真实模型开关。benchmark 增加版本化 manifest、micro/node/scenario/pipeline/transport profile、逐 attempt 证据、source/environment/fixture/config fingerprint、cold/warm Renderer、冻结 gate、AI-off/显式 AI-on、聚合报告与 baseline comparison；现有 M5 固定 10 例、gate 和盲评继续独立。V1.0 默认关闭 HTTP Lab、只写 Lab Artifact，策略 Memory 只 preview；第一版是否同时做前端页面、后续是否允许真实 Memory 写入保留为实现前用户确认项。本次仅修改设计、决策和进度 Markdown，没有创建或修改运行时代码；`make docs-check` 通过，节点目录与实际 Graph 逐名比对为 19/19，F09 仍是唯一 active。

- 2026-07-14：完成 M6.0 可靠性修复与四路审计收口。模型角色增加 60/120/60/45/90 秒 cap 和 10%（最多 30 秒）下游 reserve；Initial 固定绑定和常量倒序 `smoothstep` 只做受限确定性意图修复并重新跑完整 Validator；Renderer 成功但 Evaluator 超时/异常时返回 `unscored_fallback=true`、`score=null`、`metrics_url=null` 的 WebGL-valid 结果。Backend 生成失败改为类型化 envelope，区分 timeout、供应商、配置、响应、Renderer、persistence、validation 和内部 pipeline；终态 events/logs/run update 使用同一事务、行锁和幂等冲突检查，成功账本故障不再遮蔽可返回 Shader。Legacy 默认不捕获/打印 reasoning，并有 180 秒服务端 timeout；WebGL compiler 原文只留私有 Artifact，事件只写长度与 SHA。Frontend 默认 Legacy，实验 V1 显示 no-go，支持停止等待与预设级浏览器 timeout，并准确显示 WebGL fallback。验证：相关聚焦回归 70 passed；`make check` 通过（227 个单元测试、docs-check、3 个 LangGraph、Frontend production build）；全量集成 15 passed、1 skipped，其中真实 Chromium WebGL 回归 2 passed；`e2e:procedural-v1`（正常评分 + fallback）和 `e2e:memory` 均通过；全仓 Ruff、`mypy --strict src/`（81 个源文件）、M6.0 文件格式检查和 `git diff --check` 均通过；对事故 run 的原 Shader 本地修复 4 处后静态 Validator 通过。未调用真实模型。

- 2026-07-14：完善 Graph 可视化维护机制。在每个 Builder 邻近的 ASCII 图之外，`src/agent/app/graphs/ARCHITECTURE.md` 新增 3 个可渲染 Mermaid 图和 PNG-to-Shader 全量条件路由表，并突出 `current_best` 安全边界；`make docs-check` 新增 AST 静态检查，自动发现任一 `*_graph.py` 缺少源码图、Mermaid 区块、节点、直接边或字面量条件边的问题，且不启动模型或浏览器。验证：harness 定向测试 11 passed；相关 Ruff、`make docs-check`、`uv run langgraph validate`（3 graphs）和 `git diff --check` 均通过。当时完整 `make check` 为 217 passed、4 failed，原因是新增 `unscored_fallback` fixture 和 reasoning 安全日志断言尚未同步；这些契约漂移已由上方 M6.0 条目修复，当前门禁以 227 passed 为准。
- 2026-07-14：为全部 3 个 LangGraph 构建文件（`main_graph.py`、`shader_generation_graph.py`、`png_to_shader_v1_graph.py`）补充与实际 `add_edge` / `add_conditional_edges` 一一对应的 ASCII 流程注释。图中明确了 `operation`、`model_node_outcome`、`route_next_action` 的分支，以及 V1 的 `current_best` 安全边界；实现行为和功能状态未改变。验证：Graph 定向集成测试 8 passed；Python 语法检查、图文件 Ruff、`make docs-check` 和 `git diff --check` 均通过。
- 2026-07-14：完成最新 `procedural_v1` 失败 run `80c54e7c-573e-4a3e-b814-48539c77ff53` 的只读事故审查，未修改运行时代码。PostgreSQL、LangGraph checkpoint 和 Artifact 共同证明：balanced 的 300 秒预算中，VisualAnalysis 用时 38.457 秒；Author 首次输出用时 46.183 秒，仅因 initial 固定绑定字段不合法，又触发同模型整份 JSON repair 84.226 秒；首候选四处 `reversed_smoothstep_edges` 被静态门禁拒绝；compile-repair 获得剩余 128.640 秒后超时，最终无 current_best。对原 Shader 在内存中执行四处语义等价的确定性 smoothstep 修复后，静态检查、真实 WebGL1 编译和渲染均通过，`total_loss=0.131508`，证明该失败本可在不调用模型的情况下恢复。审查同时确认 F09 正式 M5 gate 已是 `failed`/灰度 no-go，但前端仍默认选择 `procedural_v1`；全局 wall budget 没有下游保留量；失败账本混淆结构化 Parser 错误与 GLSL Validator 错误、批量事件缺少发生时间；终态事件/日志/run 更新不在事务内；阻塞 HTTP 无实时进度、取消或失败 run_id；SDK 默认最多重试 2 次但预算只计逻辑调用。当前常规门禁仍全绿：`make check` 188 passed，集成测试 13 passed、1 skipped，全量 Ruff 和 `mypy --strict src/` 通过，说明主要缺口在真实延迟、预算生存性、失败恢复和发布门禁，而非基础静态质量。下一步应先完成 P0 稳定性修复并重跑固定真实基准，再继续视觉质量优化。
- 2026-07-14：修复真实 run `4cb1b13a-0a10-484a-90a3-c1c392668e0e` 在 Agent 成功后仍返回 HTTP 500 的响应契约问题。阶段日志证明第三个候选已通过 WebGL1，`total_loss=0.087040`、`quality_threshold_met`；失败发生在 `ShaderScore` 构造，因为 metrics Artifact 直接序列化 `ScoreBreakdownV1` dataclass，三个内部 pair tuple 映射被 JSON 编码成 list，而 API 要求 object。现改为写入 `score.to_dict()`，Agent service 对旧 pair-list Artifact 做兼容归一化；Backend 先构造完整响应再写 `status=succeeded`，响应契约失败会打印 `shader.generate.response_contract_failed` 并按 failed 入账，避免“数据库成功、HTTP 500”。同时将生成终态事件/日志从逐条远程写入改为 asyncpg 批量写入，移除本次 finalize 后约 55 秒尾延迟对应的逐条往返路径；实际远程耗时改善需以下一次真实 run 为准。旧 run 的实际 metrics 已做无模型兼容验证，三个字段均恢复为 dict 并通过 `ShaderScore`，loss 保持 `0.087040101...`。验证：定向 Ruff 通过，相关单元/集成测试 23 passed；`make check` 通过（188 个单元测试、docs-check、3 个 LangGraph、前端生产构建）；全量集成测试 13 passed、1 skipped；全量 Ruff 通过；`mypy --strict src/` 通过（81 个源文件）。本次未追加真实模型调用。
- 2026-07-14：修复一次真实 `procedural_v1` balanced 请求在 329 秒后返回 422 的问题，并补齐可定位到阶段的后端日志。数据库 run `b6fa41c4-a084-4999-8ac3-4600e4990d3b` 证明 Graph 的真实停止原因为 `wall_time_exhausted`：首次 VisualAnalysis 仅因 `regions_of_interest[1].purpose` 非法而失败（46.803 秒），整份 JSON 模型修复又耗时 156.668 秒，Author 只剩约 97 秒后被取消，最终 `candidate_count=0`。现对全部且仅为 ROI purpose 的显式已知别名执行 `visual_analysis_roi_purpose_alias_v1` 本地归一化，并重新跑完整严格 Parser；未知值仍走原模型修复，不放宽 Parser。新增 `shader.pipeline.*` 模型/渲染/评估/finalize 阶段日志和 `shader.generate.*` 请求终态日志；失败安全摘要同时进入终端、`agent_logs` 和 `agent_runs.result`，包含停止原因、失败阶段/事件/类型、候选数、模型耗时和 validation codes，不含图片、完整 GLSL、reasoning、模型原文或密钥；FastAPI route 前置 422 另由 `request.validation_failed` 打印字段路径、类型和提示。验证：定向新增/相关测试 28 passed；`make check` 通过（186 个单元测试、docs-check、3 个 LangGraph、前端生产构建）；全量集成测试 13 passed、1 skipped；全量 Ruff 通过；`mypy --strict src/` 通过（81 个源文件）。本次未追加真实模型调用，避免未经显式确认产生费用；修复路径由 Fake Gateway 证明从两次模型调用降为一次。
- 2026-07-14：修复远端 PostgreSQL 关闭闲置连接后，LangGraph 专用 psycopg pool 仍把陈旧连接交给 checkpointer、导致 `psycopg.OperationalError: consuming input failed` 和 generate 503 的问题。过程账本 asyncpg 同时写入成功且 `/health/db` 正常，证明不是数据库整体不可用；根因是 `backend.app.database.agent_memory._pool()` 的 checkout `check` 为空。现已配置 `AsyncConnectionPool.check_connection`，坏连接在进入 Graph 前淘汰并重建，不对整个 Graph 做可能重复模型调用的盲重试。Backend/配置定向测试 34 passed，Ruff 通过，实际 pool 验证 `checkout_check_enabled=True`；受控关闭已归还池的连接后再次执行只读查询，得到 `stale_replaced=True`、`fresh_connection=True`。功能状态不变。
- 2026-07-14：修复 `src/agent/app/graphs/png_to_shader_v1_graph.py` 文件首字符被误写为 `s"""` 导致 `make setup-memory-postgres` 在导入阶段触发 `SyntaxError` 的启动阻塞。全目录 Python 语法检查通过，PNG-to-Shader routing/Graph 定向测试 13 passed，数据库初始化模块导入通过；随后重新执行 `make setup-memory-postgres` 成功，输出 `Agent Memory PostgreSQL setup completed.`。`npm install` 的 `fsevents` allow-scripts 提示为非阻塞的 macOS 可选文件监听警告。功能状态不变。
- 2026-07-14：完成 `m5-20260713-balanced-v3` 独立人工盲评入库与最终门禁复算。提交 JSON 的 suite run、10 个唯一 case 和 `A | B | TIE` 选择均通过校验；原文件按原字节归档为 `blind-review/human-review.json`，SHA-256 为 `2dbaaaf833ae6eabfa0c8161fbc924810231297c83412f380bdde910cd641f50`，原冻结 config SHA-256 保持 `38c9db801263f0e3c015c8f08a0f9c5ec968c6ae90360f4e97a8cf5f8d714340`。选择结果为 9 个平局、1 个 final；人工完整度 `10/10` 通过，final 偏好率 `1/10=10%` 低于 50% 门槛。runner 报告新增 human-review SHA-256，载入评审后不再显示待评审提示；目标单元测试 7 passed、Ruff 通过。最终 gate 仍为 `failed`，与自动指标 no-go 一致，F09 保持 active、禁止灰度。
- 2026-07-13：完成 `F09` M5 工程实现和正式自动验收，灰度结论为 no-go。新增 `shaderforge.benchmark`、版本化 `m5_gate.yaml`、确定性 AI-off baseline、显式按量 AI-on runner、全局调用上限、逐 case 原子证据、旧运行恢复保护、失败/中断保留、报告聚合、匿名 A/B 盲评页和 nightly workflow；结构化三角色及 JSON repair 改为 `temperature=0`、`thinking=off`、`capture_reasoning=false`、`response_format=json_object`，WebGL1 静态契约补充禁止 derivative builtins。正式 run `m5-20260713-balanced-v3` 的 10/10 AI-off 与 AI-on 均通过静态校验和真实 Chromium WebGL1 编译，traceability、final=current_best 与 best 单调性均为 100%；实际审计模型为 `dashscope:qwen3.7-plus`，共 70 次调用、302285 input tokens、92920 output tokens，平均每例 7.0 次调用、266.83 秒、1.2 次 best 更新。只有 `color_lobes` 达到至少 0.005 的 initial-final 改善，改善率 `1/10=10%`，低于冻结的 70%；AI-on benchmark loss 仅 `2/10` 不劣于 AI-off。`pink_gel` 的主体 bbox 无法可靠测得、global RMSE `0.236844>0.16`，四个关键 ROI 均超阈值，因此自动 gate 明确为 `failed`，未移动阈值。报告 schema v2 记录 config/manifest/gate SHA-256，并区分旧配置快照和逐调用实际模型身份；新运行在调用前冻结三角色模型、thinking 与 response format，旧 schema v1 的不完整 AI-on run 禁止继续以避免混用模型。正式盲评页已用 Playwright CLI 验证为 `0/10`、下载禁用、10 组 reference/A/B 共 30 张资源全部 HTTP 200，未代替评审人投票；截图为 `output/playwright/m5-blind-review-final.png`。最终验证：`make check` 通过（182 个单元测试、docs-check、3 个 LangGraph、前端生产构建）；全量集成测试 13 passed、1 skipped；全量 Ruff 和 `mypy --strict src/` 通过（81 个源文件）；`procedural_v1` 与 Memory 两条真实 Chromium E2E 均通过；最新 10 例 `make benchmark-ai-off` 通过；E2E shell 语法、nightly workflow YAML 和 `git diff --check` 均通过。F09 保持 active、禁止灰度。
- 2026-07-13：完成 `F09` M4 Backend、Frontend 与过程账本接入。`POST /api/shader/generate` 新增 `legacy | procedural_v1`、`fast | balanced | high` 和补充约束，V1 响应返回 run、current_best、停止原因、评分、规范化 render 尺寸及 final-render/metrics/manifest URL；无有效候选返回安全的 422/503，不泄露 reasoning。新增 `agent.app.services.png_to_shader_v1`，V1 checkpoint 使用 `png-to-shader-v1:{project_id}` 与 legacy 隔离、共享项目 Store；LocalArtifactStore 新增持久 run 索引，HTTP 只允许三种固定 Artifact，拒绝任意路径。Graph finalize 复制并校验 `final/shader.frag`、`final/render.png`、`final/metrics.json`、`final/manifest.json`；过程账本写入 generation mode、质量档位、Graph 阶段、模型审计、`current_best_updated` 和最终摘要。前端默认显式 V1，支持质量档位/约束、运行阶段摘要、服务端 render、评分、停止原因和最后一次自动 Review；客户端以 WebGL1、`u_time=0`、无参考图纹理重新编译，并按服务端规范化尺寸计算 RGB RMSE。成功晋升策略时保留服务生命周期声明的 `durable | ephemeral | degraded`，避免把可写的进程内 Store 误报为持久记忆。验证：`make check` 通过（174 个单元测试、docs-check、3 个 LangGraph、前端生产构建）；普通集成测试 12 passed、1 skipped；全量 Ruff 与 `mypy --strict src/` 通过（75 个源文件）；`npm --prefix frontend run e2e:procedural-v1` 在真实 Chromium 中完成上传到结果展示并得到客户端/服务端 RMSE `0.0000`，截图为 `output/playwright/procedural-v1-final.png`；修正 E2E 元素定位和零匹配假阳性后，`npm --prefix frontend run e2e:memory` 也通过；两个 E2E shell 均通过语法检查，`git diff --check` 通过。未调用真实模型；F09 保持 active，下一步为 M5。
- 2026-07-13：完成 `F09` M3 有界 LangGraph 闭环。新增独立并注册到 `langgraph.json` 的 `png_to_shader_v1_graph`，接入 `prepare_context`、M2 三角色和 M1 事实层；新增 CandidateRecord/current_best 纯选择器，只有硬约束通过、总损失至少改善阈值且保护区不超退化才晋级。每个候选原子保存 GLSL、Author、provenance、compile、render、metrics、selection、review 和 manifest；Critic/refine 只从 current_best Artifact 重载绑定证据，finalize 也只从 best Artifact 读取 GLSL/PNG/metrics。模型包装器按剩余预算把 JSON 修复限制为 0/1 次，模型/Renderer 调用受 wall-time timeout 约束；compile、visual、model、wall-time、源码、Renderer replay、V1 high ceiling 和 Graph recursion 均有硬上限。Renderer 按 project/run 隔离复用并在 finalize 关闭；只有经 Renderer、Oracle、Selector 验证的 best 策略可晋升 Memory，未验证 compile failure 明确不晋升。验收覆盖首候选成功、compile repair、退化候选保 best、两轮无提升停止、模型失败保 best 和循环上限；真实 Chromium WebGL1 跨 Graph/Oracle/Artifact/Memory 路径通过。验证：`make check` 通过（全量单元测试 164 passed、docs-check、LangGraph validate 发现 3 个图、前端构建）；普通集成测试 11 passed、1 skipped；M3 定向组合 37 passed；M3 相关 10 个源码文件 `mypy --strict` 通过；全量 ruff 通过。未调用真实模型，M3 未改 Backend/Frontend；F09 保持 active，下一步为 M4。
- 2026-07-13：完成 `F09` M2 三个模型角色与结构化 Parser。新增版本化 VisualAnalysis、Author initial/compile_repair/visual_refine、VisualCritic 和 structured-output-repair Prompt；新增不可变 Pydantic 契约、完整 JSON/单一 fence 严格 Parser、重复 key/非有限数/未知字段/角色越权/版本与候选绑定校验，以及不调用静态 Validator 的 compile-repair scope guard。三个 Node 只注入 `LLMGateway`，语义调用默认 `temperature=0`、thinking on，唯一一次 JSON 修复强制 thinking off；Critic/refine 调用前校验 candidate、GLSL、render 三方 SHA-256，model_calls/Candidate provenance 同时记录 requested/actual model、身份来源、Prompt/repair 版本、attempt、usage、原输出 hash 与 GLSL hash。修正 Gateway 原先把请求模型 ref 当作实际身份的审计缺口，响应元数据优先、配置身份仅作显式 fallback。验证：M2 定向 Parser/Node/Prompt 测试 41 passed；全量单元测试 150 passed；`make check` 通过（docs-check、2 个既有 Graph、前端构建）；普通集成测试 4 passed、1 skipped；`uv run ruff check src/agent src/shaderforge backend tests scripts` 通过；M2/Gateway 相关 9 个源码文件 `mypy --strict` 通过。未调用真实模型，M2 未新增 Graph/Backend/Frontend 路径；F09 保持 active，下一步为 M3。
- 2026-07-13：完成 `F09` M1 模型无关事实层。新增 `analysis/` TargetMeasurements（白底 alpha 合成、主体 bbox/置信度、调色板、代表像素、边缘和 ROI）、`validation/` WebGL1 无贴图 Validator、`rendering/` 可复用 Playwright/Chromium WebGL1 worker、`evaluation/` Basic Oracle 和 `store/` 原子 LocalArtifactStore；Renderer 固定新 canvas/context、`u_time=0`、无纹理绑定和一次 worker 重放，结构化保留静态违规、compile/link/draw log、console error 与浏览器/GL 元数据。新增粉色凝胶 golden Shader 和 `scripts/render_m1_golden.py`，产物写入 `output/playwright/m1-golden/`；`render.png` SHA-256 为 `475fd4984f95d8cdc63adea7a3ba5fad6a218b32509ceacb49fb370487dd3170`，相对 M0 pink_gel 参考图的 `basic_oracle_v1 total_loss=0.0871419474`。验证：M1 定向单元测试 36 passed；WebGL 集成测试证明重复渲染 PNG 字节一致、非法 GLSL 有 fragment log 且无 PNG、失败后不泄漏旧帧；`make check` 通过（106 个单元测试、docs-check、2 个 LangGraph graph、前端构建）；完整普通集成测试 4 passed、1 skipped；`uv run ruff check src/agent src/shaderforge backend tests scripts` 通过；`uv run mypy --strict src/shaderforge` 通过。独立 Playwright CLI 打开 standalone HTML 后状态为 `ready`、`success=true`、`drawError=null`，控制台 0 error/0 warning，截图为 `output/playwright/m1-golden/browser-page.png`。F09 继续保持 active，下一步为 M2。
- 2026-07-13：初始化 Git 仓库并发布到 `https://github.com/TOoSad-deep/ShaderGen`。首个提交为 `e3ee677 chore: initial import`；新增 GitHub Actions 单元测试与集成测试工作流随项目一同提交。`.gitignore` 已补充本地 IDE、浏览器自动化和代理工作状态规则，未提交 `.env`、依赖目录或本地缓存；保留源码、文档、基准图片和可复现的 `output/` 产物。发布前执行 `make check` 通过：70 个单元测试通过、`docs-check` 通过、LangGraph 配置有效（2 个图）、前端生产构建通过。功能状态和架构契约未改变。
- 2026-07-13：完成 `F09` M0。新增首个真实 `src/shaderforge/` 子包及稳定公共入口，冻结 `webgl1_static_no_texture_v1`、8 个问题域、9 个停止原因、3 个质量档位、硬预算和 `current_best` 接受策略；`image_to_glsl.yaml` 已删除“必须 texture2D”冲突并改为版本化无贴图 Prompt，Generate Node 从 YAML 定义写入 `image_to_glsl_no_texture_v1` 审计版本。新增标准库 benchmark 生成器、10 张 192×192 确定性 PNG、能力/ROI/bbox 容差 manifest 和 SHA-256 固定证据；新增 7 个 M0 契约测试。验证：M0 定向测试 7 passed；全量单元测试 70 passed；普通集成测试 3 passed、1 skipped（PostgreSQL 专用测试按设计跳过）；`uv run ruff check src/agent src/shaderforge backend tests scripts` 通过；`make docs-check` 通过；`make check` 通过并包含 LangGraph validate 发现 2 个图和前端构建。抽查 pink_gel、arc_highlight_orb、neon_ring、rounded_rect_glow 图片显示正常。F09 继续保持 active，下一步为 M1，M1–M5 未完成前不得标记 passing。
- 2026-07-13：完成 `F08` Shader Memory 与 Context Engineering 实现。Agent 新增版本化 `MemoryItem`、幂等 Review upsert/分页清除、纯 `ContextPolicy`/`ContextPack` GSSC Builder、`prepare_context`/`promote_memory` Node 和显式 generate/review 路由；任务 checkpoint 只保留 project、phase、iteration、GLSL hash、生成模型/时间和 Review fallback，图片、渲染图、完整 GLSL、ContextPack、选中 ID、memory status、run ID、模型调用、事件、日志和 reasoning 均使用 `UntrackedValue`。Backend 新增独立 psycopg saver/store 生命周期、严格 msgpack、独立 setup 命令、`project_id` 过程账本、单进程项目锁、Generate/Review memory status 和清除 API；无数据库时明确降级为临时记忆。Frontend 支持当前/最近 10 个 project ID、刷新恢复、新建/恢复/清除和 ephemeral/degraded 提示。验证：63 个单元测试通过；普通集成测试 3 个通过、PostgreSQL 专用测试按普通套件设计跳过；`make test-memory-postgres` 在自动创建的随机隔离数据库中通过资源重建恢复测试（1 passed），finally 删除测试数据库并复核残留数为 0；`uv run ruff check src/agent backend tests scripts` 通过；`make check` 通过；Playwright CLI 浏览器 E2E 完成上传、生成、canvas Review、刷新恢复、新建、最近项目恢复和清除，`npm --prefix frontend run e2e:memory` 通过，最终截图为 `output/playwright/memory-final.png`。未执行真实模型调用。已对配置的 ShaderGen 数据库执行 `make setup-memory-postgres`，运行时健康检查返回 `memory_status=durable`。当前目录没有 Git 元数据，无 commit。
- 2026-07-13：用户确认开始实现 `F08`。已先同步实现前审查结论到规格与 D018，并把 `F08` 从 `not_started` 改为 `active`；后续按 Memory/Context 纯模块、Graph/State、Backend persistence/API、Frontend 项目恢复、PostgreSQL/浏览器验收的顺序推进。当前尚未完成代码与验收，不得标记为 passing。
- 2026-07-13：完成现有 Shader Memory / Context Engineering 规格的实现前设计审查，结论为总体方向保留，但进入 `F08` 前需要修订若干契约：将单次运行的 `selected_memory_ids` / `memory_status` 移出 checkpoint；删除或明确定义当前纯 GLSL 输出无法产生的 `last_generation_summary`；为 `MemoryItem` 增加 schema 版本和更新时间并定义确定性 importance/upsert 语义；按当前 GLSL/iteration 约束 Review 选择以避免历史建议冲突；明确 checkpoint 成功而 Store 失败时的幂等重试；把 Store namespace 清除实现为分页逐项删除；将 2,000 token、50 条候选等参数收口为有默认值的策略配置；PostgreSQL 首次 setup 与生产迁移分离，并启用严格 msgpack 反序列化。另建议用本地 recent project ID 列表解决“长期保留但新建后不可返回”的孤儿项目体验。当前只完成审查，未修改设计规格、代码、API、功能状态或决策记录，`F08` 保持 `not_started`。验证：`make docs-check` 通过。当前目录没有 Git 元数据，无 commit。
- 2026-07-13：复核 Shader Memory 与 Context Engineering 规划及当前代码事实。确认现有 `F08` / `D018` / `docs/superpowers/specs/2026-07-10-memory-context-design.md` 仍与仓库现状一致：Shader Graph 尚未接入 checkpointer/store，Generate 与 Review 仍是独立请求，API 尚无 `project_id`，`ShaderPipelineState` 仍携带图片、完整 GLSL 和过程摘要。对照 Hello-Agents 第八章分层记忆与第九章 GSSC，继续采用“借鉴方法、不引入其运行时依赖”的最小方案：任务内轻量 checkpoint、按 `project_id` 隔离的筛选后长期 Memory、约 2,000 token 的确定性 GSSC；首期不引入 embedding、向量库、图数据库、LLM 摘要或图片持久化。依赖可行性检查确认当前 `langgraph==1.2.7`、`langgraph-checkpoint==4.1.1`，`UntrackedValue` 和 `StateGraph.compile(checkpointer=..., store=...)` 可用；PostgreSQL persistence 包尚未安装，应在 F08 实现阶段加入。当前只完成规划复核，`F08` 保持 `not_started`。验证：`make docs-check` 通过。当前目录没有 Git 元数据，无 commit。
- 2026-07-13：新增 `human_doc/图片复刻无贴图Shader方法论与技术细节.md`，将静态粉色玻璃圆片案例整理为可复用的“参考图 -> 无贴图 Shader”方法论。文档补充运行时契约、参考图量化、椭圆 SDF、Gaussian 分层、方向与径向高光、rim/haze/sheen、WebGL1 `mediump` 数值安全、完整中文注释版 GLSL、Playwright 验证骨架、Pillow/NumPy 指标脚本、四轮 RMSE 迭代、停止条件与常见失败模式；未修改 Shader、前端、后端、Prompt、API、架构或功能状态。验证：`make docs-check` 通过。当前目录没有 Git 元数据，无 commit。
- 2026-07-10：完成静态粉色玻璃圆片 Shader 的真实 WebGL1 验证及终审修复。5 处 radial Gaussian 均改为先计算 `(radial-target)/sigma` 再平方，避免最低 WebGL1 `mediump` 下小差值先平方的潜在下溢；结构 RED 检查先确认 5 个归一化量缺失，修复后 GREEN 检查通过。保留既有阴影参数，在最多两轮边界内调整现有色团、rim 与双侧高光，并仅新增一个内部 haze 和一个细内侧 sheen：数值修复基线 RMSE `14.4407`，候选 1 为 `11.4405`，最终候选 2 为 `10.8290`；最终参考 bbox `(38, 32, 488, 501)`、渲染 bbox `(37, 31, 489, 502)`，四边均相差 1 px。Playwright CLI wrapper 在真实 WebGL1 中编译、链接、绘制成功，页面为 `ready`，最终控制台 0 errors / 0 warnings；`output/static_pink_glass_orb.png` 为 505×527，临时预览页、浏览器 session 和静态服务器均已清理。`view_image` 复核显示内部雾化、细亮边、缩短的左上高光和纯白右下高光均较终审前改善；残余差异为深左代表点略偏浅、左上高光蓝通道仍偏低，以及解析层次仍比参考平滑。验证：最终 Shader/产物/清理约束扫描通过；`make docs-check` 通过。本次未修改前端、后端、Prompt、API 或 `docs/FEATURES.md`；当前目录没有 Git 元数据，无 commit。
- 2026-07-10：完成 Shader Memory 与 Context Engineering 设计讨论。确认采用 LangGraph 原生 checkpointer/Store、`project_id` 隔离、Hello-Agents GSSC 思路但不引入其依赖、任务内自动 checkpoint 与经筛选的项目长期记忆；首个闭环接入 Shader 生成/评审图。规格写入 `docs/superpowers/specs/2026-07-10-memory-context-design.md`，新增未开始功能 `F08` 和 D018 决策。首期没有项目列表，切换到新项目后 UI 不能返回旧 project ID，必须先提示用户按需清除当前项目记忆。当前只修改文档，尚未进入实现；设计规格待用户书面复核。验证：`make docs-check` 通过。当前目录没有 Git 元数据，无法提交 commit。
- 2026-07-10：根据项目当前已验证能力整理阶段总结，新增 `7.10阶段总结.md`。内容覆盖仓库事实来源、分层边界、图片生成 GLSL 与在线 Review 最小闭环、模型配置、过程数据、验证体系及后续缺口；未改变功能状态、架构或 API 契约。验证：`make docs-check` 通过。
- 2026-07-10：完成 Agent 模型层到 LLM Gateway 的目录和依赖重构。删除 `agent.app.models`，新增中立 `agent.app.contracts.llm`、具体 `agent.app.llms`、`llms/families` 和复用消息 `messages`；`LangChainLLMGateway` 统一真实模型身份、耗时、reasoning、token usage 与安全错误。基础对话、GLSL 生成和渲染评审 Node 均通过构造参数依赖 `LLMGateway`，Graph Builder 负责注入具体实现；State 和 `model_calls` 的模型名改为只使用 `LLMResponse.model_ref`，修复节点配置模型可能只写审计元数据而未控制真实调用的问题。图片消息 helper 迁移到 `messages`，reasoning 日志迁移到 `observability`，新增 AST 架构测试阻止 Node 回连具体 LLM 实现。验证：`uv run pytest tests/unit_tests -q` 通过（57 个测试）；`uv run pytest tests/integration_tests -q` 通过（1 个测试）；`uv run ruff check src/agent backend tests scripts` 通过；`make docs-check` 通过；`uv run langgraph validate` 通过并发现 2 个 graph；`make check` 通过并包含前端构建。未执行带密钥的真实模型调用。当前目录没有 Git 元数据，无法提交 commit。
- 2026-07-08：按用户反馈拆分模型供应商配置，并修正 provider 与 model family 混用问题。新增 `src/agent/app/models/provider_config.py` 维护供应商 API key/base URL；`qwen_model.py`、`glm_model.py`、`deepseek_model.py`、`openai_model.py` 分别维护模型系列专属参数和特殊响应处理；`llm_factory.py` 改为先解析 `provider:model` 中的 provider，再按真实模型名识别 Qwen/GLM/DeepSeek/OpenAI 系列，例如 `dashscope:qwen3.7-plus` 使用 DashScope 凭据但进入 Qwen 配置。`model_config.py` 只保留通用默认模型名和布尔环境变量解析。同步补充 README 环境变量、Agent config/models 架构说明和 D013 决策记录。验证：先新增 provider 拆分和路由测试并确认 6 个预期失败；实现后目标测试通过；随后针对 `dashscope:` 不等于 Qwen 的问题新增 4 个回归测试并确认 3 个预期失败；修复后目标测试通过。未执行带密钥的真实模型调用。
- 2026-07-08：完成 Qwen 模型工厂配置调整。`get_model()` 改名为 `get_qwen_model()`；新增 `SHADER_GEN_QWEN_ENABLE_THINKING`，为空时沿用模型默认、设为 true/false 时通过 DashScope OpenAI-compatible `extra_body.enable_thinking` 显式开关 thinking；新增 `SHADER_GEN_QWEN_OUTPUT_THINKING`，默认不保留 `reasoning_content`，显式开启时放入 `AIMessage.additional_kwargs["reasoning_content"]`。同步更新 README、Agent config/models 架构说明和 D012 决策记录。验证：先新增 5 个工厂/配置测试并确认失败，再实现后目标测试通过；`uv run ruff check src/agent/app/config/model_config.py src/agent/app/models/llm_factory.py tests/unit_tests/test_configuration.py` 通过；`uv run pytest tests/unit_tests -q` 通过（32 个测试）；`make docs-check` 通过；`make check` 通过（32 个单元测试、docs-check、LangGraph validate 发现 2 个 graph、前端构建通过）。未执行带密钥的真实模型调用。
- 2026-07-08：按用户反馈修正 Agent 端 README 的 harness 形态。`src/agent/README.md` 从简单链接列表改为 Agent 模块接手入口，补充当前状态判断、开始前读取顺序、Agent 改动门禁、按需阅读和完成交接要求；`scripts/docs_check.py` 新增 Agent README harness router 检查；`tests/unit_tests/test_harness_contracts.py` 新增 README 契约测试和 docs-check 规则测试。同步把 `docs/FEATURES.md` 的 H01/F06 evidence 改成稳定的“单元测试通过”口径，避免硬编码测试数量导致证据反复过期。验证：先确认新增 README 契约测试和 docs-check README 规则测试按预期失败；修复后 `uv run pytest tests/unit_tests -q` 通过（27 个测试），`make docs-check` 通过，`uv run pytest tests/integration_tests -q` 通过（1 个测试），`uv run ruff check src/agent backend tests scripts` 通过，`make check` 通过（27 个单元测试、docs-check、LangGraph validate 发现 2 个 graph、前端构建通过）。
- 2026-07-08：修复 harness/Agent 架构审查 findings。`F06` 收窄为 Agent/后端在线 Review 能力，新增 `F07` 承载浏览器端 “canvas 截图 -> review API -> UI 展示” 自动化缺口；新增 `scripts/docs_check.py` 与 `make docs-check`，并接入 `make check`；新增 `tests/unit_tests/test_harness_contracts.py` 固化 feature 状态、H01 证据、Agent service 边界、State reducer 和 docs-check 契约；新增 `agent.app.parsers` 解析边界，移出 GLSL/Review 解析逻辑；`ShaderPipelineState` 的 `model_calls`、`events`、`logs` 改为 append reducer。验证：先运行新增 harness 契约测试并确认 5 个预期失败；修复后 `uv run pytest tests/unit_tests/test_harness_contracts.py -q` 通过（5 个测试），`uv run pytest tests/unit_tests -q` 通过（25 个测试），`make docs-check` 通过，`uv run pytest tests/integration_tests -q` 通过（1 个测试），`uv run ruff check src/agent backend tests scripts` 通过，`make check` 通过（25 个单元测试、docs-check、LangGraph validate 发现 2 个 graph、前端构建通过）。
- 2026-07-08：完成 harness 相关 Markdown 与 Agent/LangGraph 架构规范审查。确认本次未改功能状态；执行 `make check` 通过（20 个单元测试、LangGraph validate 发现 2 个 graph、前端构建通过），`uv run pytest tests/integration_tests -q` 通过（1 个集成测试），`uv run ruff check src/agent backend tests/unit_tests/test_configuration.py` 通过；额外扫描确认 `docs/FEATURES.md` 当前无 active 功能、后端源码未越过 `agent.app.services.*` 直接 import Agent 内部模型/Prompt/节点或 LangChain 消息类型。审查发现的待改进点以本次回复为准，尚未修改相关规范文档。
- 2026-07-08：继续按用户澄清修正文档拆分粒度。`src/agent/ARCHITECTURE.md` 改为 Agent 总览和子模块索引；新增 `src/agent/app/ARCHITECTURE.md` 以及 `config`、`graphs`、`states`、`nodes`、`prompts`、`models`、`services`、`tools`、`observability` 各目录下的 `ARCHITECTURE.md`，分别承载对应模块说明和边界；`docs/ARCHITECTURE.md` 和 `docs/DECISIONS.md` 同步说明“子模块旁边也有架构文档”。验证：`uv run langgraph validate` 通过，发现 2 个 graph；`src/agent/app` 一层子目录的 `ARCHITECTURE.md` 存在性检查通过；Markdown 引用检查通过。本次仅改 Markdown，未运行代码测试。
- 2026-07-08：按用户指出的“仓库即规范”要求拆分 Agent 文档。`src/agent/README.md` 收缩为模块入口、运行命令和事实来源索引；新增 `src/agent/ARCHITECTURE.md` 承载模块说明、目录边界、LangGraph State/Node/Edge/Graph 规则、Models/Tools/Services 边界和新增 Agent 能力流程；`docs/ARCHITECTURE.md` 的目录内规范入口改为优先指向模块旁边的 `ARCHITECTURE.md`；`docs/DECISIONS.md` 记录 D010。验证：`uv run langgraph validate` 通过，发现 2 个 graph；Markdown 引用检查和文件存在性检查通过。本次仅改 Markdown，未运行代码测试。
- 2026-07-08：根据用户提供的 LangGraph Agent Architecture Guide 更新 `src/agent/README.md`。补充 Agent 端 State、Node、Edge、Graph、Models、Tools、Services 的落地规则，并明确当前 `agent` 与 `shader_generation` 两个图、`generate_glsl_node.py` / `review_render_node.py` 的单职责节点边界、Graph 编排职责、Service 对后端暴露公共用例入口、Prompt YAML 边界和新增 Agent 能力流程。验证：`uv run langgraph validate` 通过，发现 2 个 graph；README 关键文件引用和节点文件存在性检查通过。本次仅改 Markdown，未运行代码测试。
- 2026-07-08：按用户指出的节点边界修正在线 Review 实现：新增 `src/agent/app/nodes/generate_glsl_node.py` 和 `src/agent/app/nodes/review_render_node.py` 两个独立节点文件，分别承载 GLSL 生成节点和渲染评审节点；删除混合节点文件；新增 `src/agent/app/graphs/shader_generation_graph.py`，图中先跑 `generate_glsl`，当 state 带有 `rendered_image` 和 `glsl` 时再进入 `review_render`；`langgraph.json` 注册 `agent` 和 `shader_generation` 两个图。`agent.app.services.shader_generation` 改为调用 graph，不再自己组装模型消息。补充测试证明节点分属两个文件，且 graph 的模型调用顺序是 `image_to_glsl` -> `shader_review`。验证：`make check` 通过，20 个单元测试通过，LangGraph validate 发现 2 个 graph 且无配置警告，前端构建通过；`uv run pytest tests/integration_tests -q` 通过，1 个测试通过；`uv run ruff check src/agent backend tests/unit_tests/test_configuration.py` 通过。
- 2026-07-08：完成在线 Review 节点最小实现。新增 Agent `shader_review.yaml` prompt、`review_shader_render()` 公共用例和 review JSON 解析；新增 `POST /api/shader/review`，接收 `original_file`、`rendered_file` 和 `glsl`；前端在 WebGL canvas 第一帧渲染后自动提交 review，并展示评估和建议。验证：先新增目标测试并确认因缺少 review 能力失败；实现后 `uv run pytest tests/unit_tests` 通过，18 个测试通过；`uv run pytest tests/integration_tests -q` 通过，1 个测试通过；`uv run ruff check src/agent backend tests/unit_tests/test_configuration.py` 通过；`uv run langgraph validate` 通过，仍有既有 `$schema` 警告；`npm --prefix frontend run build` 通过；`make check` 通过。
- 2026-07-08：完成项目结构 Markdown 一致性复查。补充顶层 `README.md`、`backend/README.md`、`docs/ARCHITECTURE.md` 和 `docs/DECISIONS.md` 中的新增代码落点规则：后端 route 注册到 `backend/app/api/router.py`，HTTP route 放 `backend/app/api/routes/`，schema 放 `backend/app/schemas/`，service 表达真实后端用例，SQL 放 `backend/sql/`，Agent 对后端入口放 `agent.app.services.*`，不提前创建空包。执行 Markdown 路径扫描，主事实源中未发现未标注的旧 `backend/app/routes`、`backend/app/database.py`、`backend/app/logging_config.py`、`backend/db` 或旧 Agent 平铺入口引用；剩余旧入口只出现在历史记录或“不兼容旧路径”的决策语境中。
- 2026-07-08：完成后端内部目录重构：HTTP route 迁移到 `backend/app/api/routes/`，路由聚合入口为 `backend/app/api/router.py`；数据库连接与 schema 初始化迁移到 `backend/app/database/session.py`；请求日志中间件迁移到 `backend/app/middleware/request_logging.py`；日志配置迁移到 `backend/app/core/logging.py`；Shader 响应 schema 迁移到 `backend/app/schemas/shader.py`；手写 SQL schema 记录目录迁移为 `backend/sql/`。验证：先改测试导入并运行 `uv run pytest tests/unit_tests/test_agent_process_store.py::test_initialize_database_schema_executes_sql_files -q`，确认按预期失败于 `ModuleNotFoundError: No module named 'backend.app.database.session'`；迁移后 `uv run pytest tests/unit_tests` 通过，15 个测试通过；`uv run pytest tests/integration_tests` 通过，1 个测试通过；`uv run ruff check backend tests/unit_tests/test_configuration.py tests/unit_tests/test_agent_process_store.py` 通过；`uv run langgraph validate` 通过，仍有既有 `$schema` 警告；旧后端平铺文件和旧 SQL 目录已移除。
- 2026-07-08：完成 Agent 内部目录彻底改造，`src/agent/` 改为 `src/agent/app/` 规范结构，后端改为只调用 `agent.app.services.shader_generation`，`langgraph.json` 指向 `src/agent/app/graphs/main_graph.py:graph`，Prompt 移至 `src/agent/app/prompts/*.yaml`。验证：先将测试导入改到新结构并运行 `uv run pytest tests/unit_tests/test_configuration.py::test_placeholder -q`，确认按预期失败于 `ModuleNotFoundError: No module named 'agent.app'`；迁移后 `uv run pytest tests/unit_tests` 通过，15 个测试通过；`uv run pytest tests/integration_tests` 通过，1 个测试通过；`uv run ruff check src/agent backend/app/services/shader.py tests/unit_tests/test_configuration.py tests/integration_tests/test_graph.py` 通过；`uv run langgraph validate` 通过，仍有既有 `$schema` 警告；后端边界扫描未发现 Agent 内部模型、Prompt 或 LangChain 消息导入。
- 2026-07-08：前端上传改为手动开始运行后，运行 `npm --prefix frontend run build`，结果通过；Playwright 刷新 `http://127.0.0.1:5173/` 后确认初始状态存在禁用的“开始运行”按钮，预览区显示“等待上传”。
- 2026-07-08：前端交互优化后运行 `npm --prefix frontend run build`，结果通过；使用 Playwright 打开 `http://127.0.0.1:5173/` 并检查桌面、390px 移动端截图，上传空状态和预览空状态布局正常。Playwright 控制台只有缺少 `favicon.ico` 的 404。
- 2026-07-07：`make check` 已通过。
  - `uv run pytest tests/unit_tests`：8 个测试通过。
  - `uv run langgraph validate`：配置有效，发现 1 个 graph。警告：`$schema` 不是已识别的配置字段。
  - `npm --prefix frontend run build`：通过。
- 2026-07-07：完成文档一致性修复后再次运行 `make check`，结果通过。
  - `uv run pytest tests/unit_tests`：8 个测试通过。
  - `uv run langgraph validate`：配置有效，发现 1 个 graph。警告：`$schema` 不是已识别的配置字段。
  - `npm --prefix frontend run build`：通过。
- 2026-07-07：补充 Markdown 及时更新规则后运行 `make check`，结果通过。
  - `uv run pytest tests/unit_tests`：8 个测试通过。
  - `uv run langgraph validate`：配置有效，发现 1 个 graph。警告：`$schema` 不是已识别的配置字段。
  - `npm --prefix frontend run build`：通过。
- 2026-07-07：补充不确定性处理规则后运行 `make check`，结果通过。
  - `uv run pytest tests/unit_tests`：8 个测试通过。
  - `uv run langgraph validate`：配置有效，发现 1 个 graph。警告：`$schema` 不是已识别的配置字段。
  - `npm --prefix frontend run build`：通过。
- 2026-07-07：修复 D002 和事实源列表后运行 `make check`，结果通过。
  - `uv run pytest tests/unit_tests`：8 个测试通过。
  - `uv run langgraph validate`：配置有效，发现 1 个 graph。警告：`$schema` 不是已识别的配置字段。
  - `npm --prefix frontend run build`：通过。

## 备注

- 2026-07-09：完成生成和评审节点 thinking 常开及思维链保存。`generate_glsl_node.py`、`review_render_node.py` 现在固定以 `thinking=on`、`capture_reasoning=true` 调用模型；新增 `nodes/model_reasoning.py` 提取并通过 `agent.model` logger 打印 `reasoning_content`；模型调用摘要携带 `reasoning_content`，后端写入 `agent_events.reasoning_content` 独立列，SQL 同步加入建表字段和 `ADD COLUMN IF NOT EXISTS`；`POST /api/shader/review` 也接入过程账本写库。验证：先新增/调整 6 个目标测试并确认失败，再实现后目标测试通过；`uv run pytest tests/unit_tests -q` 通过（41 个测试）；目标文件 `uv run ruff check ...` 通过；`make docs-check` 通过；`make check` 通过（41 个单元测试、docs-check、LangGraph validate 发现 2 个 graph、前端构建通过）。未执行带密钥的真实模型调用。
- 2026-07-09：完成模型 thinking 的 node 级运行时配置。新增 `models/model_options.py` 定义 `model_thinking=default/on/off` 和 `capture_reasoning` 语义参数，`nodes/model_runtime_options.py` 从 LangGraph `runtime.context` 提取配置；`shader_gen_model()` / `get_provider_model()` / `get_qwen_model()` 支持调用级覆盖，Qwen 分支负责翻译为 `extra_body.enable_thinking` 和 `output_thinking`。默认行为仍沿用环境变量，node 不传配置时不改变旧路径。验证：先新增 5 个目标测试并确认失败，再实现后目标测试通过；`uv run pytest tests/unit_tests -q` 通过（40 个测试）；目标文件 `uv run ruff check ...` 通过；`make docs-check` 通过；`make check` 通过（40 个单元测试、docs-check、LangGraph validate 发现 2 个 graph、前端构建通过）。未执行带密钥的真实模型调用。当前工作目录没有 Git 元数据，无法使用 `git status`/`git diff` 复查变更。
- 2026-07-09：重构生成和评审节点为 config 字典工厂。`generate_glsl_node.py`、`review_render_node.py` 改为 `make_generate_glsl_node(config=...)` / `make_review_render_node(config=...)` 节点工厂，`config` 字典（字段 `model`/`thinking`/`capture_reasoning`/`print_reasoning`，风格同 `model_call`）成为这两个节点模型与思维链的唯一控制源；每节点文件顶部声明默认 config 常量，图装配改为显式调用工厂。删除 `nodes/model_reasoning.py` 的 `reasoning_model_options()`；新增 `nodes/image_content.py` 抽出两节点复用的图片片段构造；两节点不再接收 `runtime` 参数。原因：原实现把这两个节点硬编码为 thinking 常开、思维链常打印，且签名里的 `runtime` 被忽略、测试反而固化了"忽略配置"，无法按节点控制是否开 thinking 或是否打印思维链；改为 config 字典后配置以 `model_call` 风格单一字典表达，"是否开 thinking"和"是否打印思维链"成为两个独立开关。默认值保持原行为（thinking 开、打印开、存库），生产行为不变；`model_calls[*].reasoning_content` 存库契约不变，`backend/` 与 SQL 不动；`model_node` 仍走 `runtime.context`，互不影响。决策记为 D016，D015 相应标注被取代。验证：改写 2 个、新增 4 个目标测试（`print_reasoning=False` 抑制打印但保留存库、`thinking="off"` 透传、`config["model"]` 流入 `model_call` 与状态）；`uv run pytest tests/unit_tests -q` 通过（45 个测试）；`uv run ruff check src/agent tests scripts` 通过；`make docs-check` 通过；`make check` 通过（45 个单元测试、docs-check、LangGraph validate 发现 2 个 graph、前端构建通过）。未执行带密钥的真实模型调用。当前工作目录没有 Git 元数据，无法使用 `git status`/`git diff` 复查变更。
- 2026-07-08：在线 Review 节点本次未执行带密钥的真实模型调用；自动化检查使用模拟模型覆盖消息内容、图片输入和 JSON 解析。浏览器自动 e2e 覆盖“canvas 截图 -> review API -> UI 展示”完整路径已从 F06 拆为 `F07`，后续实现时补 Playwright 或等价浏览器集成检查。
- 2026-07-08：排查 `POST /api/shader/generate` 返回 502。日志显示失败点在 `agent.app.services.shader_generation.generate_glsl_from_image()` 调用 `shader_gen_model().ainvoke()` 后，由 OpenAI SDK/httpx 抛出 `APIConnectionError: Connection error.`；当前终端用 curl 和 httpx 访问 DashScope 兼容接口可建立连接并收到 HTTP 响应，说明更像是当次后端进程到外部模型服务的临时网络/TLS/代理问题，而不是图片读取、GLSL 提取或后端路由逻辑错误。未执行带密钥的真实模型调用。
- 2026-07-08：讨论 `PROGRESS.md` 清理策略。建议暂不设固定周期清理，只在功能 `passing` 或阶段切换时压缩已过时细节；正式规则尚待用户确认。
- 2026-07-08：按用户反馈调整前端运行时机：上传图片只更新原图预览和文件信息，不再自动调用生成接口；用户点击“开始运行”后才请求后端，已生成后按钮显示“重新运行”。
- 2026-07-08：讨论 Agent 与前后端解耦、A/B 测试和评估 Agent 方向。初步建议是先做最小 `agent_runner` 契约和运行变体记录，不提前拆微服务；评估 Agent 先作为离线评测/建议者读取运行账本，不进入在线生成主链路。该方向尚待用户确认，未写入正式决策。
- 2026-07-08：检查当前架构解耦状态。结论：前端通过 `frontend/src/api/` 调后端、`src/agent/` 不反向依赖 `backend/`，整体分层基本成立；主要耦合点是旧结构下后端 service 直接组装 LangChain 消息并调用模型，旧 route 直接引用 agent 模型名和具体生成函数。评估集/评分 agent 建议先做离线评估集和最小评分 runner，复用现有 `agent_runs`/`agent_events` 账本，暂不做自动自我修复。
- 2026-07-08：完成后端与 Agent 边界重构：当时新增 Agent 公共接口文件作为图片到 GLSL 的入口，后端 route/service 不再直接依赖 `agent.models`、`agent.prompt_loader` 或 LangChain 消息类型；该入口已在后续彻底改造中迁移为 `agent.app.services.shader_generation`。验证：`uv run pytest tests/unit_tests` 通过，15 个测试通过；目标文件 `ruff check` 通过；`uv run langgraph validate` 通过，仍有既有 `$schema` 警告。
- 2026-07-08：讨论日志和写库职责边界。结论：浏览器前端只负责用户可见状态和必要交互埋点入口，不直接写业务库；后端负责请求日志、错误日志、数据库连接、schema 初始化和业务过程数据写入；Agent 侧只产出结构化事件/结果，由后端编排写入；后续 ShaderForge 领域层可返回评分/产物摘要，但不直接依赖 FastAPI 连接池。
- 2026-07-08：澄清 Agent 运行过程数据获取方式。后端可以拿到 Agent 日志和模型调用数据，但不应读取 Agent 内部实现；Agent 应通过公共接口返回结构化 `events`、`logs`、`model_calls` 或接受后端传入的事件记录器，由后端统一落库。当前 `ShaderGenerationResult` 只返回 GLSL 和模型名，后续如需 token、latency、prompt_version、阶段事件，需要扩展公共接口结果或事件回调。
- 2026-07-08：完成 Agent 运行过程数据解耦改造的最小版：`ShaderGenerationResult` 新增 `model_calls`、`events` 和 `logs`，Agent 公共接口返回模型调用摘要；后端成功路径把这些结构化摘要追加写入 `agent_events` 和 `agent_logs`，仍由后端统一管理数据库连接和过程账本。验证：`uv run pytest tests/unit_tests` 通过，15 个测试通过；目标文件 `ruff check` 通过；`uv run langgraph validate` 通过，仍有既有 `$schema` 警告；后端依赖扫描未发现 Agent 内部模型、Prompt 或 LangChain 消息导入。
- 2026-07-08：讨论 Agent 与后端是否物理独立。当前建议：保持同一仓库、同一后端进程内调用，但维持清晰公共接口和模块边界；暂不拆独立 Agent 服务。只有出现独立扩缩容、长任务队列、资源隔离、不同发布节奏、多语言/多运行时或故障隔离需求时，再拆成独立服务。
- 2026-07-08：完成现有前端交互优化实现：上传入口支持点击和拖拽，生成中禁用重复提交，显示文件名和大小，GLSL 区增加复制按钮，FastAPI JSON 错误改为展示 `detail` 文案。未改后端 API 契约，未扩展 F01 表单。
- 2026-07-08：完成现有前端“上传图片 -> 生成 GLSL -> WebGL 预览”交互优化设计，规格写入 `docs/superpowers/specs/2026-07-08-frontend-interaction-design.md`。本次范围不扩展 F01 的完整任务提交表单，不改变后端 API 契约。当前工作目录没有 Git 仓库元数据，无法提交 commit。
- 2026-07-07：将 Shader 生成接口中的过程数据写入编排抽离到 `backend/app/services/agent_process_store.py`，route 只调用服务层函数；写库日志来源改为 `backend.agent_process`。
- 2026-07-07：完善 Agent 过程数据写库日志。`POST /api/shader/generate` 写库成功时打印 `agent.process.database.write.succeeded`，写库失败时打印 `agent.process.database.write.failed`。
- 2026-07-07：实现 Agent 过程数据写入路径。`DATABASE_URL` 配置后后端启动会执行当时的手写 SQL 目录；新增 `backend/app/services/agent_process_store.py` 写入 `agent_runs`、`agent_events`、`agent_logs`；`POST /api/shader/generate` 在连接池可用时写入运行、事件、日志并更新运行状态。该 SQL 目录已在后续后端结构重构中迁移为 `backend/sql/`。
- 2026-07-07：在 `agent_runs` 中新增 `glsl_model_name` 和 `vision_model_name` 字段，分别记录 GLSL 生成模型和视觉分析模型型号。
- 2026-07-07：修复表结构审查问题：`agent_logs` 改用 `(run_id, event_seq)` 外键关联 `agent_events(run_id, seq)`，事件时间线改依赖 `(run_id, seq)` 稳定顺序，并明确 `debug` 只用于运行内关键调试摘要。
- 2026-07-07：审查当前 `agent_runs`、`agent_events`、`agent_logs` 表结构设计。主要风险是 `agent_logs.event_id` 未约束必须属于同一个 `run_id`，以及事件时间线索引与稳定排序字段 `seq` 不完全一致；尚未修改 schema。
- 2026-07-07：在最小 Agent 过程数据 SQL 中增加 `agent_logs` 表，用于保存挂在 `agent_runs`/`agent_events` 下的安全诊断日志摘要；普通应用日志仍继续走 Python logging。该 SQL 文件已在后续迁移到 `backend/sql/001_agent_process.sql`。
- 2026-07-07：将 Agent 过程数据 SQL 的说明和数据库注释改为中文，并在 `AGENTS.md` 补充“文档、计划、注释尽量使用中文”的长期规则。
- 2026-07-07：新增最小 Agent 过程数据 SQL，记录 `agent_runs` 和 `agent_events`，并在 `backend/README.md` 补充后端 SQL 目录规则。尚未接入自动建表或迁移执行；该 SQL 文件已在后续迁移到 `backend/sql/001_agent_process.sql`。
- 2026-07-07：完成 Agent 过程数据数据库设计分析。建议先复用现有 Postgres/`asyncpg` 入口，按 `agent_runs` + `agent_events` 建最小过程数据账本；普通应用日志继续走 logging，不直接落业务库。尚未进入实现，下一步需确认数据库目标是否包含 LangGraph 中断恢复。
- 当前代码已经有基础图片到 GLSL 路径，但尚未实现 SVG 目标流程：Routing、Agent 分析、Intent IR、DSL 节点图、Renderer、Oracle/局部损失、Search Engine、VLM/HITL 或 Store。
- 已在 `docs/ARCHITECTURE.md` 写入后续开发采用的项目结构规范：分层 monorepo，核心领域能力进入 `src/shaderforge/`，但不提前创建空目录。
- 已将前端细则写入 `frontend/README.md`，后端细则写入 `backend/README.md`，总架构文档只保留全局边界和目录规范入口。
- 已同步修复入口文档：`AGENTS.md`、`README.md`、`docs/ARCHITECTURE.md` 和 `docs/FEATURES.md` 现在都指向同一套结构边界和验证规则。
- 已补充 Markdown 及时更新规则：全局规则在 `AGENTS.md`、`README.md`、`docs/ARCHITECTURE.md`，目录细则在 `frontend/README.md` 和 `backend/README.md`。
- 已补充不确定性处理规则：影响架构、契约、数据、安全或验收且仓库事实无法判断的问题，需要先向用户确认。
- 已同步 `docs/DECISIONS.md` 的 D002 与本文件事实源列表，纳入 `README.md`、`frontend/README.md`、`backend/README.md`。
- 当前工作目录没有 Git 仓库元数据，所以进度通过文件记录，而不是通过 commit 记录。
