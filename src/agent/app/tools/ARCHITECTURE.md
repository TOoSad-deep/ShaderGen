# Tools 架构

`src/agent/app/tools/` 是 Agent 可调用外部能力的入口，例如未来的 shader 编译器、渲染器、文件工具或图像分析工具。

## 当前状态

当前没有真实工具实现，只保留模块入口。

## 边界规则

- 没有真实工具需求时，不提前创建空工具文件。
- 工具负责执行外部动作，节点负责决定何时调用工具以及如何写回 State。
- 确定性领域算法优先进入 `src/shaderforge/`，不要塞进 Agent tools。
- 如果工具依赖外部服务、文件系统或 GPU 资源，需要在测试中 mock 或提供明确集成测试边界。
