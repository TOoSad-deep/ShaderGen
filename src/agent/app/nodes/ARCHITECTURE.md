# Agent nodes

Only `layered_direct/` is part of the current product. It contains the LayerPlan
and Layered Initial/Refine authors, bounded structured-call helper, and the
single-responsibility workflow nodes called by the current LangGraph.
