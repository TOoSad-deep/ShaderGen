# 复制为 frontend/.env.local，或在启动 Vite 的 shell 中临时设置。
# 所有 VITE_* 值都会进入浏览器构建产物，禁止在此放置 API key 或其他秘密。
VITE_API_BASE_URL=http://127.0.0.1:8088

# 留空时按 quality preset 使用内置等待上限；覆盖值不得小于 10000 毫秒。
VITE_GENERATION_REQUEST_TIMEOUT_MS=
